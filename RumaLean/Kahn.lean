import RumaLean.Graph

/-!
# Kahn's Topological Sort

This module implements Kahn's Topological Sort algorithm and proves its correctness.
We also prove that when ties (multiple 0 in-degree nodes) are broken using a strict linear order,
the output of Kahn's algorithm is unique (deterministic).
-/

-- TODO: Formulate Kahn's algorithm or an abstract sorting function and prove properties.
